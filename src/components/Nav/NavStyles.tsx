export interface stylesProps {
    [index: string]: string
}
const styles: stylesProps = {
    top: '',
    bottom: 'bg-green-600 text-white p-8 rounded-2xl',
    container: `flex items-center gap-12`,
}

export default styles
