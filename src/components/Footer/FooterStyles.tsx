export interface stylesProps {
    [index: string]: string
}
const styles: stylesProps = {
    container: `footer bg-green-600 text-white py-4 px-8 rounded-2xl flex items-center gap-8`,
}

export default styles
