export interface stylesProps {
    [index: string]: string
}
const styles: stylesProps = {
    container: `footer bg-green-600 text-white p-4 rounded-2xl flex align-center gap-8`,
}

export default styles
